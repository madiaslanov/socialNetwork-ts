import st from './settings.module.css';

const Settings = () => {
    return <div className={st.main}>
        Settings: there are no endpoints corresponding to this section on the server
    </div>
}

export default Settings;

