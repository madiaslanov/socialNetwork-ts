const initialState = {

}

const dialogReducer = (state = initialState, action: any) => {
    switch (action.type) {
        default:
            return state;
    }

};

export default dialogReducer;